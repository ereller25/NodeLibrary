// Node Modules and Require
var counter = function (arr){
	return 'There are ' + arr.length + ' elements in this array';
};

console.log(counter(['evan', 'ryu', 'subzero']));

//to call in global object:
	// var counter = require('./1.3_node_modules_require') - to call this function in another file
	// above goes in the file that will call this function 
	//to make code clearer, easier to refactor
	// have to make clear what to export from the module

module.exports = counter;

